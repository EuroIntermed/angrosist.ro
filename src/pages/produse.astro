---
import CategoryGrid from '../components/CategoryGrid.astro'
import { loadCatalog } from '../lib/catalog-data'

const categories = await loadCatalog()
---

<CategoryGrid locale="ro" categories={categories} />
